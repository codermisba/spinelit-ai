# Agentic Spine AI Pipeline - dependencies
#
# Install CPU build on machines without CUDA:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
# or the CUDA build for Colab/GPU training (see https://pytorch.org).
#
# LLM agents: a Google Gemini free API key (get one at aistudio.google.com/apikey)
# is set via the GEMINI_API_KEY environment variable. The LLM client uses only
# the Python standard library, so no extra package is required. For a fully
# local option set LLM_PROVIDER=ollama and run a local Ollama server.
# For Colab training of the vision-engine imaging model also add:
#   pip install google-colab openpyxl jupyter

# Deep learning
torch
torchvision
timm

# Data / numerics
numpy
pandas
Pillow
scikit-learn
tqdm

# Structured agent outputs
pydantic

# Visualization
matplotlib

# Local UI
gradio
